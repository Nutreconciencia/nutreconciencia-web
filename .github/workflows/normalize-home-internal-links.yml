name: Normalize homepage internal links

"on":
  workflow_dispatch:

permissions:
  contents: write

jobs:
  normalize-home:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Normalize homepage internal links
        run: python scripts/normalize_home_internal_links.py

      - name: Verify canonical homepage links
        run: |
          python - <<'PY'
          from pathlib import Path
          text = Path("index.html").read_text(encoding="utf-8", errors="ignore")
          expected = [
              'href="/"',
              'href="/articulos/"',
              'href="/prensa/"',
              'href="/libro/"',
              'href="/sobre-mi/"',
              'href="/podcasts/"',
              'href="/articulos/plant-based-misinformation/"',
              'href="/articulos/omniveg/"',
              'href="/articulos/appearance-of-validity/"',
          ]
          missing = [x for x in expected if x not in text]
          if missing:
              print("MISSING:")
              for x in missing:
                  print(" -", x)
              raise SystemExit(1)
          print("All canonical homepage links verified.")
          PY

      - name: Commit
        run: |
          git config user.name "nutreconciencia-seo-bot"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet || git commit -m "fix: normalize homepage internal links"
          git push
